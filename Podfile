# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'pens' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for pens
    pod "Alamofire"
    pod "GoogleWebRTC"
    pod "SocketRocket"
    
  target 'pensTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'pensUITests' do
    # Pods for testing
  end

end
